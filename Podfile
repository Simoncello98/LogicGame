# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'logicGame' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  #levare il commento alle due righe successive per integrare firebase
  pod 'Firebase/Core'
  pod 'Firebase/Database'



  target 'logicGameTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'logicGameUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
